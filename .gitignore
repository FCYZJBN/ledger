.DS_Store
Thumbs.db
desktop.ini
node_modules/

# ---------------------------------------------------------------------------
# 真实账单 / 财务数据 —— 绝不提交
#
# 本仓库是公开仓库（GitHub Pages）。微信、支付宝导出的账单包含真实姓名、
# 手机号与全部消费记录，一旦提交即等于公开。
#
# 这些文件只应存在于本地，用于开发时验证解析器。测试一律使用合成样本。
# ---------------------------------------------------------------------------
*.xlsx
*账单*
*交易明细*
*微信支付*
*支付宝*
*流水*
/private/
/testdata-local/

# ---------------------------------------------------------------------------
# 图标原始设计稿
#
# icons/ 里只该放 App 真正在用的三张图（icon-192 / icon-512 / icon-maskable），
# 它们由 scripts/generate_icons.py 生成。原始设计稿是 810KB 的位图，
# 不进仓库 —— 本仓库是公开仓库，没必要为一张源稿付这个体积。
# 想把它纳入版本管理的话，删掉下面这行即可。
# ---------------------------------------------------------------------------
/icons/*logo*.png
